# Standards for Domain–name pairing for translation lists

No project standards files in `agent-os/standards/` apply to this work (index.yml is empty). Follow existing patterns in:

- **pages/menu-modules.php** — Section structure, checkbox usage, inline script for search/filter.
- **modules/translations.php** — Function naming (`cps_hc_gems_*`), return shape (associative arrays), option-key helper.
