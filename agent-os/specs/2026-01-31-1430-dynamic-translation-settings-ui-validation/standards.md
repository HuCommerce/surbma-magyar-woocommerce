# Standards for Dynamic translation settings UI and validation

No project standards files apply to this work. The `agent-os/standards/index.yml` is empty.

Follow existing patterns in:

- **pages/menu-modules.php** — Section structure, use of `cps_hc_gems_form_field_checkbox`, UIkit classes, and form markup.
- **settings/settings-validate.php** — Checkbox sanitization pattern (isset + 1/0), placement with other module options.
